<p>
  当前图片: <%= image_tag @item.logo.url, :style => 'height: 200px' %><br/>
</p>

<p>
  <strong>Title:</strong>
  <%= @item.title %>
</p>

<p>
  <strong>Introduction:</strong>
  <%= @item.introduction %>
</p>

<%= link_to '评论', '' %>,
<%= link_to "点赞(目前#{@item.scores.count}个赞)", '' %>
<h5>评论列表</h5>

<%= @item.comments.each do |comment|%>
  <%=  comment.body %> -- <br/>
<% end %>
