<p id="notice"><%= notice %></p>

<p>
  <strong>Logo:</strong>
  <%= @item.logo %>
  当前图片: <%= image_tag @item.logo.url, :style => 'height: 200px' %><br/>
  当前图片: <%= @item.logo.current_path %>
</p>

<p>
  <strong>Title:</strong>
  <%= @item.title %>
</p>

<p>
  <strong>Introduction:</strong>
  <%= @item.introduction %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
