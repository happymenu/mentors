<% @comment ||= Comment.new %>
<%= form_for(@comment) do |f| %>
  <div class="field">
    <%= f.text_area :body, :placeholder => '请输入评论' %>
  </div>
  <div class="actions">
    <%= f.submit '确定' %>
  </div>
<% end %>
