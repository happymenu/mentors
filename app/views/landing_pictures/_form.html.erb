<%= form_for(@landing_picture) do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => 'form-control', placeholder: '例如大V， 或者 参赛作品' %>
  </div>
  <div class="field">
    <%= f.label :logo %><br>
    当前图片：  <%= image_tag @landing_picture.logo.url, :style => 'height: 200px' %>
    <%= f.file_field :logo %>(记得严格控制尺寸, 具体尺寸请咨询美工。。。)
  </div>
  <div class="actions">
    <%= f.submit '确定', class: 'btn btn-primary' %>
    <%= link_to '返回', "/landing_pictures/admin_index", :class => 'btn btn-default' %>
  </div>
<% end %>
